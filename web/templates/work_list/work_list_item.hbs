{{#each workList}}
	<a class="list-group-item" id="{{id}}" data-name="{{name}}">
		<button class="work-delete-btn glyphicon glyphicon-remove"></button>
		{{name}}
		<div class="glyphicon glyphicon-link"></div>
	</a>
{{/each}}